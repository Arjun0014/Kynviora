node_modules/
dist/
build/
.expo/
*.log
.env
.env.*
!.env.example
coverage/
.DS_Store
Thumbs.db
*.tsbuildinfo
.turbo/

# Local development database (BLK-001 workaround; PGlite persisted to disk).
# Any directory, not just the default: a dogfooding run with its own
# KYNVIORA_LOCAL_DB_DIR once put 1,277 PGlite pages into a commit.
.kynviora-data/
.kynviora-*/
